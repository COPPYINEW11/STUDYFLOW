#### StudyFlow — AI 기반 스터디 플래너 앱

#### 개발 플랫폼: Google Antigravity IDE
#### 기본 언어: 한국어
#### 첫 실행 화면: 시간표 화면
#### 지원 언어: 한국어, 영어, 일본어, 중국어 간체/번체, 베트남어

---

#### 앱 구조
- 시간표 화면 (기본 시작 화면)
  - 시간표 탭
  - 일정표 탭
  - 캘린더 탭
- 타이머 화면
    - 뽀모도로 모드
    - 타이머 모드
    - 스톱워치 모드
- 그룹 화면 
- 통계 화면
  - 레벨 / 업적 / 통계 그래프(일간, 주간, 월간) / 목표 달성률
- 설정 화면
  - 다크모드 / 알림 / 타이머 / 언어 / 스터디 그룹 / 로그인

---

#### AI 기능

### 자연어 일정 등록
- 텍스트 입력만으로 일정 자동 파싱 및 저장
- 예: "내일 오후 3시 수학 2시간" → 날짜·시간·과목 자동 인식 후 일정 등록
- 예: "오늘 저녁 7시 영어 단어 암기 1시간" → 일정표에 자동 추가
- 장소도 함께 입력 가능 (예: "내일 오후 3시 수학 2시간 / 학교 도서관")
- 오른쪽 하단에 AI모양의 플로팅 버튼에 텍스트를 입력하면 AI가 일정을 자동 파싱 및 저장
  - 플로팅 버튼을 클릭하면 키보드가 활성화되고 텍스트를 입력할 수 있음

### 이미지 → 시간표 자동 생성
- 시간표 사진 업로드 시 AI가 과목명·요일·시간대 자동 추출
- 파싱 결과 미리보기 화면 → 사용자 확인 → 시간표 저장

### 메모 자동 정리
- 메모 저장 시 AI가 태그 자동 분류 및 과목 연결
- 텍스트만 입력해도 AI가 자동으로 과목 연결 및 태그 생성 (설정에서 AI 메모 정리 ON/OFF 설정 가능)


### 학습 조언
- 통계 데이터 분석 기반 취약 과목·집중 패턴 추천
- AI가 직접 학습 패턴을 추천함 (설정에서 AI 학습 조언 ON/OFF 설정 가능)

### 공부 시작 알림
- 일정표 연동, 다음 일정 시작 전 자동 알림 생성

---

#### 탭바 (하단 네비게이션)

### 구성
- 시간표 탭
- 타이머 탭
- 통계 탭
- 설정 탭
- 그룹 탭

### 애니메이션
- 아래로 스와이프: 탭바가 아래로 슬라이드 아웃 (몰입 모드 진입)
- 위로 스와이프: 탭바가 아래에서 위로 슬라이드 인 (복귀)

---

#### 시간표 화면

### 공통 UI
- 상단 헤드라인에 현재 레벨 표시 (예: Lv.1 (유저이름))
- 3분할 세그먼트 컨트롤로 시간표 / 일정표 / 캘린더 탭 전환
- 탭 전환 중에도 AI 어시스턴트 항시 활성 상태 유지

### 시간표 탭

## 기본 기능
- 이미지 업로드 시 AI가 시간표 자동 파싱 및 생성 (과목명, 요일, 시간대 자동 인식) ( 장소 입력 가능 )
- 특정 과목 칸을 길게 누르면 해당 과목 타이머 즉시 시작
  - 타이머 시작 시 모드 선택 가능: 뽀모도로 / 타이머 / 스톱워치
- 현재 시간과 연동된 빨간색 수평 바 표시 (오늘 날짜 강조)

## 자물쇠 기능
- ON 상태: 시간표 편집 잠금, 임의 수정 불가
- OFF 상태: 자유 편집 가능

## 시간표 초기화
- 휴지통 아이콘 탭 → 초기화 확인 다이얼로그 표시
- 확인 버튼 클릭 시 전체 시간표 삭제

### 일정표 탭

## 화면 구성
- 하루 전체 일정을 타임라인 형식으로 표시
- 상단: 현재 진행 중인 일정 (과목명 + 해야 할 내용 + 남은 시간 + 장소)
- 하단: 다음 일정 미리보기 (과목명 + 시작 시간 + 장소)
- 장소 입력 시만 장소 표시

## 일정 추가
- 텍스트 자연어 입력으로 AI 파싱 후 자동 등록 ( 할일, 과목, 시간, 장소)
- 장소도 함께 입력 가능 (예: "내일 오후 3시 수학 2시간 / 학교 도서관")

#### 일정 삭제
- 일정 스와이프 시 삭제

### 캘린더 탭

## 화면 구성
- 현재 날짜 기준 월간 캘린더 표시
- 일정이 있는 날짜에 컬러 도트 표시

## 인터랙션
- 단순 탭: 해당 날짜의 일정 목록 슬라이드업 표시
- 롱탭: 해당 날짜의 일정표 전체 화면으로 열기

---

#### 타이머 화면

### 모드 구성
- 뽀모도로 모드: 기본 집중 25분 / 휴식 5분 (기본값)
- 타이머 모드: 사용자 지정 시간 설정
- 스톱워치 모드: 사용자 지정 시간 설정

### 시계 클릭 시
- 뽀모도로 모드일 때 : 집중 시간 / 휴식 시간 표시 모달
- 타이머 모드일 때 : 타이머 시간 표시 모달
- 스톱워치 모드일 때 : 타이머 시간 표시 모달
- 모달에서 시간을 조정 할 수 있음
- 모달은 아래에서 위로 올라오는 애니메이션 효과 사용

#### 뽀모도로 설정
- 집중 시간 사용자 임의 설정 가능
- 휴식 시간 사용자 임의 설정 가능

### 타이머 동작 방식
- 타이머 시작 시 전체 화면(집중 모드 UI)으로 전환
- 휴대폰 뒤집기 감지 시 타이머 활성화
- 화면을 다시 올리면 타이머 일시정지 (설정에서 ON/OFF 가능)
- 종료 시 진동 또는 알림음 재생 (설정에서 선택)

### 타이머 제어
- 시작 / 일시정지 / 정지 버튼 
- 종료 시 과목에 공부한 시간 + 휴식 시간 자동 저장 


### 휴식 기록 기능
- 타이머 일시정지 또는 종료 시 휴식 유형 선택
  - 식사 / 휴식 / 기타 (직접 커스텀 입력 가능) (일시정지 클릭 시 어떤 휴식인지 묻는 팝업창 표시)
- 선택된 유형과 시간이 통계에 자동 저장


---

#### 통계 화면

### 통계 표시
- 일간 / 주간 / 월간 공부 시간 그래프
- 가장 많이 공부한 과목 Top 3
- 최장 집중 기록 (단일 세션 기준)
- 휴식 유형별 시간 분포
- 현재 레벨 및 경험치 진행 바

### 레벨 시스템

#### 경험치 규칙
- 5분 공부 시 50EXP 획득
- 누적 EXP 기준 레벨업 (레벨별 필요 EXP 점진적 증가)
- 현재 레벨 시간표 화면 헤드라인에 항시 표시
- 1000EXP 당 1레벨

#### 업적 시스템
- 시작: 10분 공부하기
- 꾸준함: 7일 연속 공부 기록
- 집중력: 한 번에 2시간 이상 공부
- 노력가: 누적 100시간 공부
- 마스터: 누적 1000시간 공부
- 소통: 그룹 참가
- 협동: 그룹 목표 3회 달성
- 리더: 그룹 생성

---

#### 그룹 화면

### 그룹 참여
- 초대 코드 생성 및 공유로 그룹 참여
- 초대 코드 수신 시 서로의 공부 시간 공유 활성화

### 그룹 기능
- 그룹원 최대 5명
- 그룹 목표 설정 (예: 수학 공부 1시간, 영어 공부 1시간) 
- 멤버별 실시간 공부 시간 현황판 (서로의 공부 시간 확인 가능)
- 그룹 목표 달성률 진행 바 (그룹원 개별 적용)
- 그룹 목표 달성 시 달성한 멤버에게 공부한 5분당 exp 2배 추가 지급 (예: 5분 공부 시 100 exp)


---

#### 설정 화면

### 테마
- 다크모드 ON / OFF 토글
- 기본 제공 테마 변경 가능 (추후 테마 추가 예정)
- 설정 내 테마 변경 시 앱 전체 테마 변경

### 집중 모드 알림
- 집중 모드 알림 ON / OFF 토글
- 공부 중 허용할 앱 알림 개별 선택 가능 (휴대폰에 설치된 앱 목록을 불러와 선택 가능)
- 선택된 앱 외 모든 알림 차단

### 타이머 설정
- 휴대폰 뒤집기를 해야 타이머 시간이 흐르는 기능 ON / OFF
- 타이머 종료 알림 방식 선택: 진동 / 알림음


### 언어 설정
- 한국어 (기본값)
- 영어
- 일본어
- 중국어 간체 / 번체
- 베트남어

### 개발자 도구
- 관리자 only 기능
- 여러 기능을 사용할 수 있는 개발자 도구 추가
  - 예: 모든 과목 제거, 모든 데이터 삭제,타이머 시간 5분 감소, 스톱워치 5분 증가, 뽀모도로 시간 5분 증가, 휴식 시간 5분 증가, 레벨 1 감소, 레벨 1 증가, 휴식 유형 추가/삭제, 그룹원 추가/삭제, 그룹 목표 변경, 그룹 목표 달성 초기화 등등 

### 로그인 / 계정 연동
- 로그인 시 시간표, 타이머, 통계, 스터디 그룹 정보가 서버에 저장
- 로그인 중엔 로그인된 여러 기기끼리 데이터 동기화
- 회원탈퇴 시 시간표, 타이머, 통계, 스터디 그룹 정보가 모두 삭제됨

#### 지원 로그인 방식
- 카카오톡 계정
- 네이버 계정
- 구글 계정
- 이메일 계정 (자체 회원가입)